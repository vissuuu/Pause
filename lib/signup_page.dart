import 'package:flutter/material.dart';

class SignupPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: Implement the UI for the signup pages
    return Scaffold(
      body: Center(
        child: Text('Signup Page'),
      ),
    );
  }
}
